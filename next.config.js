/** @type {import('next').NextConfig} */

module.exports = {
    // Uncomment to use images from the net
    // images: {
    //     domains: ['i.ibb.co'], // change this url if needed
    // },
    reactStrictMode: true,
    swcMinify: true,
};
